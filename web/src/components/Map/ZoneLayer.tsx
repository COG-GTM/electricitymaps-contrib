// Note: zone fill / outline / hover layers are constructed inline inside
// ElectricityMap so that they share the same GeoJSON source and remain
// reactive to the current carbon-intensity dataset. This file is kept as a
// re-export point in case those layer specs need to be reused (e.g. for
// printing or thumbnail rendering).
export {};
